<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Personagem</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">


    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            padding: 20px;
        }

        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        .character-info {
            margin-bottom: 20px;
        }

        .character-info h2 {
            margin-top: 0;
        }

        .character-info img {
            max-width: 100%;
            border-radius: 4px;
        }

        .edit-form {
            margin-top: 20px;
        }

        .edit-form label {
            font-weight: bold;
        }

        .edit-form input {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .edit-form button {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            cursor: pointer;
        }

        .edit-form button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <% if (typeof error !== 'undefined') { %>
<div class="alert alert-danger" role="alert">
          <%= error %>
        </div>
      <% } %>

    <div class="container">
        <div class="character-info">
            <h2>Editar Personagem</h2>
            <img src="<%= character.image %>" alt="<%= character.name %>" />
            <h3><%= character.name %></h3>
        </div>
        <form action="/editCharacter" method="POST" class="edit-form">
            <input type="hidden" name="id" value="<%= character.id %>">

            <label for="name">Nome:</label>
            <input type="text" name="name" id="name" value="<%= character.name %>" placeholder="Nome atual: <%= character.name %>" required>
            <label for="status">Status:</label>

            <input type="text" name="status" id="status" value="<%= character.status %>" placeholder="Status atual: <%= character.status %>" required>
            <label for="species">Espécie:</label>

            <input type="text" name="species" id="species" value="<%= character.species %>" placeholder="Espécie atual: <%= character.species %>" required>
            <label for="type">Tipo:</label>

            <input type="text" name="type" id="type" value="<%= character.type %>" placeholder="Tipo atual: <%= character.type %>">
            <label for="gender">Gênero:</label>

            <input type="text" name="gender" id="gender" value="<%= character.gender %>" placeholder="Gênero atual: <%= character.gender %>" required>
            <label for="origin">Origem nome:</label>

            <input type="text" name="originName" id="origin" value="<%= character.originName %>" placeholder="Origem atual: <%= character.originName %>" required>
            <label for="origin">Origem link:</label>

            <input type="text" name="originLink" id="origin" value="<%= character.originLink %>" placeholder="Origem atual: <%= character.originLink %>">
            <label for="location">Localização nome:</label>

            <input type="text" name="locationName" id="location" value="<%= character.locationName %>" placeholder="Localização atual: <%= character.locationName %>" required>
            <label for="location">Localização link:</label>
            
            <input type="text" name="locationLink" id="location" value="<%= character.locationLink %>" placeholder="Localização atual: <%= character.locationName %>" required>
            <label for="image">URL da Imagem:</label>

            <input type="text" name="image" id="image" value="<%= character.image %>" placeholder="URL da imagem atual: <%= character.image %>" required>
            <label for="created">Data de Criação:</label>

            <input type="text" name="created" id="created" value="<%= character.created %>" placeholder="Data de criação atual: <%= character.created %>" required>
            <label for="url">URL:</label>

            <input type="text" name="url" id="url" value="<%= character.url %>" placeholder="URL atual: <%= character.url %>" required>
            <button type="submit">Salvar Alterações</button>
        </form>
    </div>
</body>
</html>
